version: "1.0"
name: permissive
rules:
  # Deny rules first
  - name: credential-files
    tools: [Read, Write, Edit]
    path_patterns:
      - "\\.env$|\\.env\\."
      - "\\.ssh/|\\.aws/|\\.gnupg/"
      - "\\.key$|\\.pem$|\\.p12$|\\.pfx$|\\.keystore$"
      - "id_rsa|id_ed25519|id_ecdsa|id_dsa|credentials|secrets?|token\\.json$"
    action: deny
    reason: "Credential/sensitive file access blocked"

  - name: no-force-push
    tool: Bash
    command_patterns: ["git\\s+push.*(-f|--force)"]
    action: deny
    reason: "Force push blocked even in permissive mode"

  - name: destructive-bash
    tool: Bash
    command_patterns:
      - "\\bsudo\\b"
      - "\\b(curl|wget)\\b.*\\|.*\\b(bash|sh|zsh)\\b"
      - "\\b(DROP|TRUNCATE)\\s+(TABLE|DATABASE)\\b"
    action: deny
    reason: "Destructive command blocked"

  # Recursive delete asks rather than denies: in practice the target is the
  # agent's own litter (build dirs, caches, logs), and a hard deny ended the
  # whole job to protect a cache directory. An approval keeps a human in the
  # loop without ending the work; nothing here runs unattended. `sudo`,
  # pipe-to-shell, SQL `DROP` and force-push stay denied above.
  - name: recursive-delete
    tool: Bash
    command_patterns:
      - "(?:^|[;&|`(\\n]|\\$\\()\\s*(?:sudo\\s+)?rm\\s+(?:-\\S+\\s+)*-\\S*[rR]\\S*[fF]"
      - "(?:^|[;&|`(\\n]|\\$\\()\\s*(?:sudo\\s+)?rm\\s+(?:-\\S+\\s+)*-\\S*[fF]\\S*[rR]"
      - "(?:^|[;&|`(\\n]|\\$\\()\\s*(?:sudo\\s+)?rm\\s+(?:-\\S+\\s+)*-[rR]\\s+(?:-\\S+\\s+)*-[fF]\\b"
      - "(?:^|[;&|`(\\n]|\\$\\()\\s*(?:sudo\\s+)?rm\\s+(?:-\\S+\\s+)*-[fF]\\s+(?:-\\S+\\s+)*-[rR]\\b"
      - "(?:^|[;&|`(\\n]|\\$\\()\\s*(?:sudo\\s+)?rm\\s+(?:-\\S+\\s+)*--recursive\\b"
    action: require_approval
    reason: "Recursive delete — confirm the path before it runs"
    risk_level: high

  # `credential-files` above is scoped to Read/Write/Edit, so it never sees a
  # shell command: `cat <repo>/.env` and `echo … >> ~/.ssh/authorized_keys` are
  # otherwise cleared outright by `safe-bash` below, and claude's native deny
  # globs — the only thing that used to stop them — are anchored at `~/` and
  # installed by the tmux runtime alone.
  #
  # One credential path per pattern, unanchored, so the tmux runtime mirrors
  # each as a narrow `permissions.ask` glob (`Bash(*.ssh/*)`) instead of
  # prompting on every `cat`. Patterns are matched against what the shell
  # executes, quoting respected, so a path named inside a quoted remote payload
  # (`ssh host 'ls ~/.ssh/'`) is not a local credential access and does not
  # match.
  - name: credential-bash
    tool: Bash
    command_patterns:
      - "\\.env\\b(?!\\.(example|sample|template|dist))"
      - "\\.ssh/"
      - "\\.aws/"
      - "\\.gnupg/"
      - "authorized_keys"
      - "\\.pem\\b"
      - "\\.key\\b"
      - "\\.p12\\b"
      - "\\.pfx\\b"
      - "\\.keystore\\b"
      - "id_rsa"
      - "id_ed25519"
      - "id_ecdsa"
      - "id_dsa"
      - "\\.git-credentials\\b"
      - "credentials\\b"
      - "secret\\."
      - "secrets\\."
      - "token\\.json\\b"
    action: require_approval
    description: "Shell access to a credential path"
    risk_level: critical

  # Allow rules
  - name: agent-internal-tools
    tools: [AskUserQuestion, ExitPlanMode, Task, TaskCreate, TaskUpdate, TaskList, TaskGet, TodoWrite, EnterPlanMode, Skill]
    action: allow

  - name: all-reads
    tools: [Read, Glob, Grep, WebSearch, WebFetch, LS, ToolSearch]
    action: allow

  - name: plan-file-writes
    tools: [Write, Edit]
    path_patterns:
      - "\\.plan$"
      - "\\.claude/plans/"
    action: allow

  - name: all-writes
    tools: [Write, Edit, NotebookEdit]
    action: allow

  - name: safe-bash
    tool: Bash
    command_patterns:
      - "^(ls|cat|head|tail|find|grep|wc|du|df|pwd|echo|date|whoami|which|type|file|stat)\\b"
      - "^git\\s+(-[a-zA-Z]\\s+\\S+\\s+|--[a-z-]+(=\\S+)?\\s+)*?(status|log|diff|show|branch|remote|tag|add|commit|stash)\\b"
      - "^(npm|yarn|pnpm|bun|pip|uv|cargo|go|make)\\s"
      - "^(python|node|ruby|java|rustc|gcc)\\s"
      - "^(pytest|jest|vitest|cargo test|go test)\\b"
    action: allow

  - name: browser-tools
    tools: [browser_navigate, browser_navigate_back, browser_navigate_forward, browser_snapshot, browser_take_screenshot, browser_console_messages, browser_network_requests, browser_click, browser_type, browser_hover, browser_drag, browser_press_key, browser_select_option, browser_file_upload, browser_handle_dialog, browser_fill_form, browser_evaluate, browser_tabs, browser_tab_list, browser_tab_new, browser_tab_select, browser_tab_close, browser_resize, browser_wait_for, browser_pdf_save, browser_close, browser_install, browser_generate_playwright_test]
    action: allow
    description: "Browser tools (permissive mode)"

  - name: agent-browser-privileged
    tool: Bash
    command_patterns:
      - "^agent-browser\\s+(batch|chat|confirm|connect|dashboard|deny|install|mcp|plugin|stream|upgrade)\\b"
      - "^agent-browser\\s+doctor\\b.*--fix"
    action: require_approval
    description: "Privileged agent-browser operation (code install, listener, real-Chrome attach, third-party send)"

  # Permissive clears every agent-browser subcommand, but the pipe target is
  # not an agent-browser operation: `… | curl -d @-` uploads whatever the page
  # read, and a matching allow rule means nothing after the pipe is examined.
  - name: agent-browser-all
    tool: Bash
    command_patterns:
      - "^agent-browser\\b[^|]*(\\|\\s*(jq|head|tail|wc|grep|rg|sort|uniq|column|cat|cut|tr)\\b[^|]*)*$"
    action: allow
    description: "Agent-browser tools (permissive mode)"

  - name: git-mutations
    tool: Bash
    command_patterns:
      - "^git\\s+(-[a-zA-Z]\\s+\\S+\\s+|--[a-z-]+(=\\S+)?\\s+)*?(push|reset|rebase|merge|cherry-pick|rm)\\b"
    action: require_approval
    description: "Git mutation"

settings:
  default_action: require_approval
  approval_timeout_seconds: 600
